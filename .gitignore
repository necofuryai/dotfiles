# Claude Code machine-local state
/.claude/settings.local.json
/.claude/.cc-writes/

# macOS Finder metadata (also in ~/.gitignore_global; kept here for other clones)
.DS_Store

# nvim writes nvim.log to its cwd when it cannot reach its state dir (sandboxed runs)
nvim.log
